{% extends "base.html" %}
{% block content %}

<div id="examples">

    <p class="desc">Listing examples by rule categories help organize examples in to groups 
        related to the markup or functional accessibility feature of a web resource.  
        The rule caegories roughly matchup with HTML markup, so the grouping labels should help developers more easily find
        examples of interest to them.  
    </p>
        

    <table aria-labelledby="id_h2_summary">
       <thead>
           <tr>
               <th class="text">Rule Category</th>
               <th class="num"><abbr title="Number of Examples in Rule Category">Examples</abbr></th>
           </tr>
       </thead>
       <tbody>
         {% for rc in rule_categories %}
         <tr {% if forloop.counter|divisibleby:"2" %} class="even" {% else %} class="odd" {% endif %}>
           <td><a href="{{ rc.get_examples_url }}">{{rc.title_plural}}</a></td>
           {% if rc.get_example_count %}
           <td class="num">{{ rc.get_example_count }}</td>
           {% else %}
           <td class="num zero">0</td>
           {% endif %}
         </tr>    
         {% endfor %}    
       </tbody>       
   </table>
    
    <p class="desc">NOTE: Examples may be associated with more than on rule category depending on features of the example</p>
    
</div>
{% endblock %}

{% block nav_options %}
{% include 'examples/example_options.html' %}
{% endblock %}
